{
  "name": "bug-log",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:client && npm run setup:server",
    "setup:client": "cd bug-log.client && npm i",
    "setup:server": "cd bug-log.server && npm i",
    "start": "node bug-log.server/index.js"
  },
  "engines": {
    "node": "^14.15.4"
  },
  "dependencies": {
    "nodemon": "^2.0.7"
  }
}
